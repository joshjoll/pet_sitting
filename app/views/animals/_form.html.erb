<%= form_with(model: animal) do |form| %>
  <% if animal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
        <% animal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :animal_type, style: "display: block" %>
    <%= form.text_field :animal_type %>
  </div>

  <div>
    <%= form.label :hourly_cost, style: "display: block" %>
    <%= form.number_field :hourly_cost %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
